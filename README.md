# go-postgres_dockercompose

Docker compose builds and run Golang app + Postgresql database. Web site on Go shows current time on Postgresql.

Download project and run command:

docker compose up --build -d


Then, enter the website:

http://localhost:8080
